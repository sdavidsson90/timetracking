# --------------------------------------------------
source('actions/requirements.R')

# --------------------------------------------------
log <- read_log()

# --------------------------------------------------
hrs <- date_to_sec(log[,"checkout"]) - date_to_sec(log[,"checkin"])
log[, "hrs"] <- sec_to_hm(hrs)

# --------------------------------------------------
write_log(log)
